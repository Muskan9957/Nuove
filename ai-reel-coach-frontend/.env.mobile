# Env for the native mobile build only (vite build --mode mobile).
# The app can't use same-origin/relative URLs (it loads from https://localhost),
# so every API + the streaming edge function must be absolute.
VITE_API_URL=https://viralcoach-production.up.railway.app
VITE_WEB_URL=https://nuove.in
